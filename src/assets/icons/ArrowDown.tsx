import LordIcon from "@/utils/LordIcon"
import React from "react"

const ArrowDown = (props: any) => {
	return <LordIcon name="arrow-down" {...props} strokeWidth="1rem" />
}

export default ArrowDown
